export const metadata = {
  title: "Register",
  description:
    "Create a DocAppoint account and start booking doctor appointments.",
};

export default function SignUpLayout({ children }) {
  return children;
}

