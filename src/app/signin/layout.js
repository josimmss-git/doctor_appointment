export const metadata = {
  title: "Login",
  description:
    "Sign in to your DocAppoint account to manage appointments and profile.",
};

export default function SignInLayout({ children }) {
  return children;
}