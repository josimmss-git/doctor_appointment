export const metadata = {
  title: "My Bookings",
  description: "View and manage your booked doctor appointments.",
};

export default function MyBookingsLayout({ children }) {
  return <>{children}</>;
}